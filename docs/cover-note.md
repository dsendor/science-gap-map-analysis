# Cover note — draft, not sent

**Status: draft.** External communication is David's. Nothing here has been sent to
anyone at Convergent Research.

The page carries the argument, the caveats and the ask, so this is deliberately short.
Its only job is to get the link opened.

---

To: gapmap@convergentresearch.org
Subject: A contribution to the Gap Map - two gaps broken into critical paths

Hi -

You said v1.0 isn't comprehensive "at all" and asked for constructive and critical
feedback, so here's a contribution rather than a comment.

I'm a data science leader with 20+ years using data and AI to optimise complex
processes, most recently leading Enterprise Data Science at Liberty Mutual. As AI has
improved I've moved into AI for science, trying to work out where it actually
accelerates discovery rather than where it's merely busy. I started building a gap map
of my own to think it through, and then found you'd already built a much better one.

So I built on yours instead. Two additions, both on top of your 2026-07-29 export, keyed
on your own ids and slugs, with none of your data touched:

1. Break each gap into its critical path, so you can see whether and how each
   foundational capability accelerates each step.
2. Label how AI can and does affect each gap, and how mature that AI is.

I did the first properly for one gap, research publishing, and the result I didn't
expect came out of your data rather than my labels. Map the four foundational
capabilities you list for that gap onto the seven steps and they cluster on the last
two. Four of the seven have nothing attached at all, and one of those four is reviewer
recruitment, where editors are now sending 4.5 invitations for every accepted review.
So which step is the money actually in? On this chain it's the one with nothing built
for it, which isn't something a one-line label on the whole gap can tell you.

**[link]**

Fair warning on scope: one gap has been read end to end by a person and the other 102
are a model's first pass, labelled that way throughout. Treat it as a probe, not a
survey!

I'd love to talk about scaling this to the rest of the map. Does the approach resonate?

David Sendor
david@sendorai.com

---

## Notes for David, not for sending

- **Everything factual regenerates from the database**: 103 gaps, 369 capabilities, the
  seven publishing steps, the four capabilities on that gap, the four steps with none,
  and the 2026-07-29 snapshot date.
- **4.5 invitations per accepted review** is Silverchair, *Future of Peer Review 2026*,
  from eight years of ScholarOne activity. It is the one number in the email and it is
  doing the work of the whole argument, so it needs to survive being looked up.
- **The 20+ years figure** conflicts with the site footer, which says 15+. Pick one. The
  email currently follows David's own draft.
- **Voice check**: 2 question marks, 1 exclamation mark, 10 contractions in ~320 words.
  The contraction target is ≥20 per 1,200 words, which is ≥5 here, so it clears. No em
  dashes: the skill calls for spaced hyphens.
- **Who to send to.** `gapmap@convergentresearch.org` is the address on their front page
  for questions and comments, and their contribute page is the other invitation this
  email is answering. `sarah@convergentresearch.org` is listed separately for funders,
  which is a different conversation and the wrong door for this.
- **Aaron Tohuvavohu** is verified twice: as a resource cited by the capability "Space
  Telescope Factory", which attaches to the telescope gap, and by name in the
  acknowledgments on gap-map.org/about. If a warm entry point is wanted later, there is
  a named person already associated with one of the two worked gaps.
