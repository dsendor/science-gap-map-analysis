# Copy to .env and fill in. .env is gitignored and must never be committed.
#
# The predecessor repo (ai-science-gap-map) committed a live Brave key directly
# into .mcp.json, where it sat in public git history. Hence this file, and hence
# .mcp.json being gitignored here.
BRAVE_API_KEY=
