node_modules/
.next/
out/

# Generated by engine/export-artifact.mjs, which `npm run build` runs first.
# Same rule as db/gapmap.sqlite: the source of record is the pinned snapshot plus
# the JSON in research-log/, both reviewable in a diff. A 500KB generated blob is not.
public/data.json
public/gap-map-augmented.csv
