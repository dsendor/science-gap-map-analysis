import './globals.css';

export const metadata = {
  title: 'Gap Map, augmented — outcomes, AI capability types, measurability, two critical paths',
  description:
    "An additive augmentation of Convergent Research's Fundamental Development Gap Map v1.0: a stated outcome, an AI capability type and maturity, and a measurability tier for all 103 gaps, plus a sampled set of progress indicators, four proposed gaps and two worked critical paths.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
